namespace Common.FlowField
{
    // Keeps the edit-mode assembly explicit even when a test runner is not installed.
    internal static class FlowFieldEditModeContractAnchor
    {
        internal const string AssemblyContract = "Common.FlowField.Core";
    }
}
